- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:

#- create:
#  - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]

