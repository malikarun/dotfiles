- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/nvim/init.vim:
      create: true
      path: init.vim

- create:
    - ~/code

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]

