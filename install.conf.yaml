- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/nvim/init.vim: init.vim

#- create:
#  - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]

