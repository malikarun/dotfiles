- defaults:
    link:
      relink: true

- clean: ['~']

  #- link:
  #    ~/.tmux.conf: tmux.conf
  #    ~/.vim: vim
  #    ~/.vimrc: vimrc

- create:
  - ~/work
  - ~/work/at/home

  #- shell:
  #  - [git submodule update --init --recursive, Installing submodules]

