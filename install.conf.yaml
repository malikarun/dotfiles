- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/nvim/init.vim: nvim/init.vim

- create:
    - ~/code

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]

