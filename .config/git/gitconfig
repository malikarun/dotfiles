[pull]
        rebase = true
[push]
        autoSetupRemote = true
[rebase]
        autoStash = true
        autosquash = true
[user]
        name  = Arun Malik
        email = 3743409+malikarun@users.noreply.github.com
[alias]
        gpf   = git push --force-with-lease
[core]
        editor       = nvim
        pager        = (which delta &> /dev/null && delta --color-only --24-bit-color=never --max-line-distance 1.0 || cat) | ${PAGER:-less}
        excludesfile = ~/.gitignore
[interactive]
        diffFilter = (which delta &> /dev/null && delta --color-only --24-bit-color=never --max-line-distance 1.0 || cat)
[includeIf "gitdir:envision/"]
        path = ~/.config/git/gitconfig-work
